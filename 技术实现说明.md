# 小羊毛：技术栈与关键实现

## 1. 技术栈

| 层级 | 技术 | 作用 |
| --- | --- | --- |
| 页面 | 原生 HTML5、CSS3、ES Modules JavaScript | 保持轻量、无前端框架依赖，适合手机优先 PWA |
| UI | CSS Grid、CSS 自定义属性、媒体查询 | 手机两列、平板三列、桌面四列，卡片高度统一 |
| PWA | Web App Manifest、Service Worker | 添加到桌面、独立窗口、离线打开首页 |
| 发布 | GitHub + Vercel Hobby | `main` 分支推送后自动生产部署，静态资源走 CDN |
| API | Vercel Node.js Functions | 同源入口读取、创建、编辑、暂停/恢复和确认接口 |
| 数据库 | Neon Serverless Postgres | 保存入口、状态、排序、检查时间和登录失败计数 |
| 数据访问 | `@neondatabase/serverless` | 使用参数化 SQL 与事务，适配 Serverless 运行时 |
| 安全 | Node `crypto`（scrypt、HMAC、随机数） | 密码哈希、签名会话、Cookie 和登录限流 |
| 测试 | Node.js `node:test` | 覆盖入口配置、链接校验、会话、静态资源和 PWA |

## 2. 目录职责

```text
index.html                 首页语义结构、PWA 元数据、管理入口
styles.css                 小羊主题、响应式网格、管理表单与焦点状态
app.js                     首页/管理页渲染、登录、API 调用、离线回退
platforms.js               14 个内置入口、分类与 HTTPS/App Scheme 校验
api/auth/                  登录、会话查询和退出接口
api/entries/               公共入口读取及管理员 CRUD 接口
api/_lib/config.js         字段校验、维护周期、同源请求和基础配置
api/_lib/db.js             Neon 连接、建表、内置入口迁移与入口查询
api/_lib/auth.js           scrypt 密码校验、HMAC 会话和登录失败锁定
db/migrations/             Neon Postgres 建表与索引 SQL
scripts/                   迁移、导出、导入和本地机密生成命令
build.mjs                  将静态资源复制到 Vercel 的 dist 目录
vercel.json                Vercel 构建目录、Function 区域和安全响应头
sw.js                      页面网络优先、静态资源缓存、API 完全绕过
```

## 3. 首页数据流

```text
打开首页
  └─ GET /api/entries
       ├─ Neon 可用：返回启用入口与待维护数量，公共响应短缓存
       └─ 数据库/网络不可用：回退 platforms.js，只读显示内置入口
```

管理页先请求 `GET /api/auth/session`。未登录时只显示密码表单；登录成功后请求 `GET /api/entries?all=1`，随后才启用新增、编辑、暂停/恢复和确认按钮。

## 4. API 接口

| 接口 | 方法 | 权限 | 用途 |
| --- | --- | --- | --- |
| `/api/entries` | GET | 公开 | 返回启用入口与待维护数量 |
| `/api/entries?all=1` | GET | 管理员 | 返回启用和暂停入口 |
| `/api/entries` | POST | 管理员 | 新增普通 HTTPS 入口 |
| `/api/entries/:id` | PATCH | 管理员 | 编辑文案、链接、颜色和步骤 |
| `/api/entries/:id/toggle` | POST | 管理员 | 暂停或恢复入口 |
| `/api/entries/:id/verify` | POST | 管理员 | 写入最新确认时间 |
| `/api/auth/login` | POST | 同源 | 校验管理密码并签发会话 Cookie |
| `/api/auth/session` | GET | 公开 | 查询当前浏览器是否已登录 |
| `/api/auth/logout` | POST | 同源 | 清除会话 Cookie |

## 5. 关键安全实现

### HTTPS 链接校验

普通入口必须是绝对 `https:` URL，且不允许用户名、密码、`javascript:` 或其他 App Scheme。服务端和前端都校验，避免绕过前端直接调用 API。

```js
export function normalizeHttps(value) {
  const url = new URL(value.trim());
  if (url.protocol !== "https:" || url.username || url.password) {
    throw new Error("跳转链接只接受不含账号密码的 HTTPS 地址");
  }
  return url.href;
}
```

淘宝唤起和京东口令只保留在首次迁移的高级入口中，管理页不能新增任意自定义 App Scheme。

### 密码与会话

密码在本地使用 `scrypt` 生成 `ADMIN_PASSWORD_HASH`。Vercel 只保存哈希，不保存明文密码。登录后使用 `SESSION_SECRET` 对包含过期时间的 Payload 做 HMAC 签名，并通过 `HttpOnly; Secure; SameSite=Strict` Cookie 保存。

连续失败 5 次会锁定 15 分钟；失败记录只保存哈希后的客户端指纹，不保存原始 IP 或密码。

### 同源写入

所有登录、退出和管理写请求都要求 `Origin` 与当前请求站点来源一致，同时要求有效会话。管理响应使用 `Cache-Control: no-store`，避免浏览器或 CDN 缓存私有数据。

## 6. 数据迁移与回滚

1. 通过 `scripts/export-entries.mjs` 从当前私有 Sites 站点导出 JSON。
2. 在 Neon 执行 `npm run db:migrate`。
3. 使用 `npm run db:import -- data/current-entries.json` 按入口 ID 幂等导入，保留启用状态、排序和确认时间。
4. 对比迁移前后数量与 ID；新 Vercel 站点验证通过后，旧私有站点继续保留作为回滚入口。

导出文件位于 `data/`，该目录已加入 `.gitignore`，不会进入公开 GitHub 仓库。

## 7. PWA 与离线策略

Service Worker 对首页采用网络优先，对 CSS、JS、Manifest 和图标采用带版本的资源缓存；`/sw.js` 自身不缓存。任何 `/api/` 请求直接交给网络层，断网时首页回退到 `platforms.js`，管理保存按钮禁用。

## 8. 验证清单

- `npm test` 通过，静态资源和四种图标均存在。
- 公开访问无需登录，管理页登录后才能读取暂停入口和写入数据。
- HTTP、凭据 URL、脚本 URL 和未知 App Scheme 保存失败。
- 购物分类不展示省钱步骤，也不生成额外网页备用按钮。
- 入口超过 30 天未确认时显示维护数量，确认后清除对应提醒。
- 生产环境检查 Vercel `.vercel.app` 地址、PWA 安装、断网首页和外部优惠页联网提示。
